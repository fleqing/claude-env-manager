# 环境变量管理工具

一个用于管理 `~/.zshrc` 文件中 ANTHROPIC_BASE_URL 和 ANTHROPIC_AUTH_TOKEN 环境变量组合的命令行工具。

## 功能特性

- 📋 **查看组合**：列出所有环境变量组合及其激活状态
- 🔄 **快速切换**：一键切换不同的环境变量组合
- ✏️ **编辑组合**：修改组合的名称、BASE_URL 或 AUTH_TOKEN
- ➕ **添加组合**：添加新的环境变量组合
- 🗑️ **删除组合**：删除不需要的组合
- 💾 **自动备份**：每次修改前自动备份，保留最近 10 个备份
- 🎨 **友好界面**：使用 rich 和 questionary 提供美观的交互体验

## 安装

1. 克隆或下载此项目
2. 安装依赖：

```bash
make install
```

或手动安装：

```bash
pip install -r requirements.txt
```

## 使用方法

运行程序：

```bash
make run
```

或直接运行：

```bash
python src/main.py
```

### 主菜单选项

1. **查看所有环境变量组合**：以表格形式展示所有组合
2. **切换环境变量组合**：选择并激活某个组合
3. **编辑组合**：修改组合的名称、BASE_URL 或 AUTH_TOKEN
4. **添加新组合**：添加新的环境变量组合
5. **删除组合**：删除指定的组合
6. **退出**：退出程序

### 使用提示

- 切换或修改环境变量后，需要执行 `source ~/.zshrc` 或重启终端使更改生效
- 所有修改操作都会自动创建备份文件
- 备份文件保存在 `~/.claude-env-manager/backups/` 目录

## 环境变量格式

工具识别以下格式的环境变量组合：

```bash
# 组合名称
export ANTHROPIC_BASE_URL=https://api.example.com
export ANTHROPIC_AUTH_TOKEN=your_token_here
```

停用的组合会被注释：

```bash
# 组合名称
#export ANTHROPIC_BASE_URL=https://api.example.com
#export ANTHROPIC_AUTH_TOKEN=your_token_here
```

## 项目结构

```
claude-env-manager/
├── src/
│   ├── __init__.py
│   ├── main.py              # 程序入口
│   ├── parser.py            # 配置文件解析器
│   ├── manager.py           # 环境变量管理器
│   └── ui.py                # 用户交互界面
├── requirements.txt         # 依赖列表
├── Makefile                 # 构建脚本
└── README.md                # 项目文档
```

## 依赖

- Python 3.8+
- questionary >= 2.0.0
- rich >= 13.0.0

## 许可

MIT License
